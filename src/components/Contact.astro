---
import {icons, contactInfo} from '../utils/contact'
import DarkButton from './buttons/DarkButton.astro'
import PrimaryButton from './buttons/PrimaryButton.astro'

---

<section id="contact" class="section-padding px-6 relative bg-dark-900/30 scroll-mt-20" data-scroll-section>
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16" data-scroll-animate>
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-4">
        Get In <span class="text-gradient">Touch</span>
      </h2>
      <div class="w-20 h-1 bg-gradient-to-r from-primary-400 to-primary-600 mx-auto rounded-full mb-4"></div>
      <p class="text-dark-400 text-lg max-w-2xl mx-auto">
        Have a project in mind or just want to chat? Feel free to reach out through any of these channels!
      </p>
    </div>

    <div class="max-w-4xl mx-auto">
      <!-- Contact Information Grid -->
      <div class="grid md:grid-cols-2 gap-6 mb-12">
        {contactInfo.map((item, index) => (
          <a
            href={item.link}
            target={item.link.startsWith('http') ? '_blank' : undefined}
            rel={item.link.startsWith('http') ? 'noopener noreferrer' : undefined}
            class="flex items-center gap-4 p-6 glass-effect rounded-xl hover:border-primary-500/50 transition-all group"
            data-scroll-animate
          >
            <div class="w-14 h-14 rounded-lg bg-primary-500/10 flex items-center justify-center group-hover:scale-110 transition-transform flex-shrink-0">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary-500">
                <Fragment set:html={icons[item.icon]} />
              </svg>
            </div>
            <div class="min-w-0">
              <p class="text-sm text-dark-400 mb-1">{item.label}</p>
              <p class="font-medium text-dark-200 group-hover:text-primary-400 transition-colors truncate">
                {item.value}
              </p>
            </div>
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-dark-600 group-hover:text-primary-500 transition-colors ml-auto flex-shrink-0">
              <path d="M7 17 17 7"/>
              <path d="M7 7h10v10"/>
            </svg>
          </a>
        ))}
      </div>

      <!-- Location Card -->
      <div class="glass-effect rounded-2xl p-8 text-center" data-scroll-animate>
        <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary-500/10 mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary-500">
            <Fragment set:html={icons.mapPin} />
          </svg>
        </div>
        <h3 class="text-2xl font-bold mb-2">Location</h3>
        <p class="text-dark-300 text-lg mb-2">Based in Madrid, Spain</p>
        <p class="text-dark-500">Available for remote work worldwide</p>
      </div>

      <!-- Call to Action -->
      <div class="text-center mt-12" data-scroll-animate>
        <p class="text-dark-300 text-lg mb-6">
          Looking forward to collaborating on your next project!
        </p>
        <div class="flex flex-wrap justify-center items-start gap-4">
          <PrimaryButton href="mailto:raul.arriola@gmail.com">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect width="20" height="16" x="2" y="4" rx="2"/>
              <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
            </svg>
            Send an Email
          </PrimaryButton>

          <DarkButton 
            href="https://linkedin.com/in/yourprofile"
            target="_blank"
            rel="noopener noreferrer">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/>
                <rect width="4" height="12" x="2" y="9"/>
                <circle cx="4" cy="4" r="2"/>
              </svg>
              Connect on LinkedIn
          </PrimaryButton>          
        </div>
      </div>
    </div>
  </div>
</section>
